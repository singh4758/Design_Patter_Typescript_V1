/**
 * Structuring the InverifyJs Container
 * Declaring Dependencies
 * Resolving some of them
 * 
 * Things we omitted from IOC Container
 *  We used magic strings for dependency names
 *  We did not have decorator available for injecting dependencies to constructot
 *  We had to provide an array of dependencies when registering the class
 */